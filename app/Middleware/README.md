# Middleware

Las funciones globales actuales de autenticacion viven en `config/session.php` por compatibilidad con rutas existentes. Las nuevas reglas transversales deben migrarse aqui de forma incremental.
